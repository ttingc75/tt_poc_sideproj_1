FROM node:22-slim

WORKDIR /app

RUN corepack enable

COPY package.json pnpm-lock.yaml* pnpm-workspace.yaml* ./
RUN pnpm install --frozen-lockfile

COPY . .

CMD ["sh", "-c", "pnpm run typecheck && pnpm run lint && pnpm run test"]
